---
hostname: arch
username: danko
user_hashed_password: "{{ vault_user_hashed_password }}"

# user:
#   name: danko
#   hashed_password: "{{ vault_user_hashed_password }}"
#   shell: fish
#   groups:
#     - wheel
#     - danko

grub_default: Windows
grub_default_fallback: saved

sudo_password: "{{ vault_sudo_password }}"

git_user_name: "{{ vault_git_user_name }}"
git_user_email: "{{ vault_git_user_email }}"
git_core_editor: "helix" # or code --wait --new-window

github_pat: "{{ vault_github_pat }}"

bitwarden_cli_state: absent # conflicts with latest nodejs, it uses lts...

binaries_path: /usr/local/bin

system_hosts_localhost_entries:
  - juicer-dev.xyz
  - mail.juicer-dev.xyz
  - s3.juicer-dev.xyz
  - fluffly-dev.xyz
  - mail.fluffly-dev.xyz
  - s3.fluffly-dev.xyz
